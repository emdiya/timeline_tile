name: example_timeline_tile
description: A new Flutter project.

version: 1.0.1+2

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_highlight: ^0.7.0
  google_fonts: ^6.1.0
  timeline_tile: ^2.0.0  # Specify the version constraint

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/

homepage: https://github.com/emdiya/timeline_tile  # Example homepage
repository: https://github.com/emdiya/timeline_tile  # Example repository
